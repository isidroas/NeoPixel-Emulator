[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"


[project]
name = "neopixel_emulator"
version = "0.0.2"
dependencies = [
    'pyglet<2',
]

[tool.setuptools]
packages=["neopixel_emulator"]